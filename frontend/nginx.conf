# Serve the built single-page app. Unknown paths fall back to index.html so a
# deep link or refresh doesn't 404.
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
